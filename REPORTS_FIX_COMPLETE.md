# ?? ????? ???????? - ???? ?????????

## ? ??????? ???????

????? ???? ???????? **??? Super Admin**? ???????? ?? ???? ?????? ????:
1. ? ??????? `[Authorize(Roles = "Super Admin,Admin,...")]` ?? ??? Endpoints
2. ? ??? Endpoints ???? ???????
3. ? ?????? ???? ?????? **Permissions** ???? **Roles** ??????

---

## ? ???? ??????

?? ????? **???? Endpoints ?? `ReportsController`** ????????:
```csharp
[RequirePermission("Reports", "view")]
```

### Endpoints ???????:

#### GET Endpoints:
1. ? `/api/reports/available-reports` ? `[RequirePermission("Reports", "view")]`
2. ? `/api/reports/assets-summary` ? `[RequirePermission("Reports", "view")]`
3. ? `/api/reports/assets-by-status` ? `[RequirePermission("Reports", "view")]`
4. ? `/api/reports/assets-by-category` ? `[RequirePermission("Reports", "view")]`
5. ? `/api/reports/assets-by-location` ? `[RequirePermission("Reports", "view")]` (??? Roles)
6. ? `/api/reports/disposal-report` ? `[RequirePermission("Reports", "view")]` (??? Roles)
7. ? `/api/reports/maintenance-report` ? `[RequirePermission("Reports", "view")]` (??? Roles)
8. ? `/api/reports/transfers-report` ? `[RequirePermission("Reports", "view")]` (??? Roles)
9. ? `/api/reports/monthly-summary` ? `[RequirePermission("Reports", "view")]` (??? ???? ???????!)
10. ? `/api/reports/assets-by-location-detail` ? `[RequirePermission("Reports", "view")]` (??? ???? ???????!)

#### POST Endpoints:
11. ? `/api/reports/assets` ? `[RequirePermission("Reports", "view")]`
12. ? `/api/reports/disposals` ? `[RequirePermission("Reports", "view")]`
13. ? `/api/reports/maintenance` ? `[RequirePermission("Reports", "view")]`
14. ? `/api/reports/transfers` ? `[RequirePermission("Reports", "view")]`
15. ? `/api/reports/summary` ? `[RequirePermission("Reports", "view")]`
16. ? `/api/reports/export/{reportType}` ? `[RequirePermission("Reports", "view")]`

---

## ?? ??? ???? ????

### 1. Super Admin
- ? ???? ?????? `Reports ? View` ????????
- ? ????? ?????? ????? ????????

### 2. Custom Roles (Admin, Manager, Employee, Viewer)
- ? ??? ??? ????? ?????? `Reports ? View` ? ?????? ??????
- ? ??? ?? ??? ????? ???????? ? `403 Forbidden`

---

## ?? ???????? ???????

1. **???? ??????** (Assets Summary)
   - `GET /api/reports/assets-summary`
   - ???????: ?????? ??????? ??????? ?????????? ?????? ?????????

2. **?????? ?????????** (Disposal Report)
   - `GET /api/reports/disposal-report`
   - ???????: ?????? ????????? ???? ???? ?????

3. **???????** (Maintenance Report)
   - `GET /api/reports/maintenance-report`
   - ???????: ????? ??????? ???? ???? ?????

4. **?????????** (Transfers Report)
   - `GET /api/reports/transfers-report`
   - ???????: ????? ??? ??????

5. **??????? ??????** (Monthly Summary)
   - `GET /api/reports/monthly-summary?year=2026&month=4`
   - ???????: ???? ???? ????

6. **????? QR Code ??? ??????**
   - `GET /api/reports/assets-by-location-detail?departmentId=1`
   - ???????: ????? ?????? ?????? QR codes

---

## ?? ????? ?????????

### ??? ?????? ???????? ???? ????:

1. ??? ???? ?? **Super Admin**
2. ???? ??? **Settings ? Roles & Permissions**
3. ???? ????? (?????: Manager)
4. ?? ??? **Reports**:
   - ? ???? **View** (??????? ????????)
5. ???? ?????????

---

## ?? ?????? ????

### ?????? 1: Super Admin
```http
GET /api/reports/assets-summary
Authorization: Bearer <super-admin-token>

? ??? ?? ???? ????????
```

### ?????? 2: Custom Role ?? ??????
```http
GET /api/reports/assets-summary
Authorization: Bearer <manager-with-reports-permission-token>

? ??? ?? ???? ????????
```

### ?????? 3: Custom Role ???? ??????
```http
GET /api/reports/assets-summary
Authorization: Bearer <employee-without-reports-permission-token>

? 403 Forbidden
```

---

## ?? ???????

- ? ???? ???????? ???? ?? Super Admin
- ? ???????? ???? ?? ?? ??? ???? ?????? `Reports ? View`
- ? ?? ???? ?????? ????? ???? ?????????
- ? ?????? ??? ????

---

## ?? ??????? ????

1. **ReportsService** ?? ???? ??? ????? ??? userId - ???????? ????
2. **?????????** ????? ??? ????? Controller ???
3. ??? ???? ????? ???????? ????? ??? ????????? ??? ????? `ReportsService`

---

## ?? ??????? ???????

1. ? **??? ????? ???????** (???? ??????? ????? F5)
2. ? **???? ?????? Reports ???? ????**:
   - Settings ? Roles ? ???? ????? ? ???? Reports ? View
3. ? **??? ???? ??????? ?? ??? ?????**
4. ? **????? ?????? ????????**

---

**?? ????? ???????! ??**
