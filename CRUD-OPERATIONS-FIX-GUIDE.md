# ?? **???? ????? ?????? CRUD ?????????**

## ?? **??????? ????????:**

???? ?????? ??????? ???????? ?????? ???? ????? authorization ??? Controllers ???? ?????? hardcoded roles ????? ?? RequirePermission.

---

## ? **Controllers ?? ???????:**

### **1. CategoriesController (?????? ??????? ???????):**
```csharp
[HttpGet] ? [RequirePermission("Categories", "view")]
[HttpPost] ? [RequirePermission("Categories", "insert")]
[HttpPut("{id}")] ? [RequirePermission("Categories", "update")]
[HttpDelete("{id}")] ? [RequirePermission("Categories", "delete")]
```

### **2. DepartmentsController (???????? ????????):**
```csharp
[HttpGet] ? [RequirePermission("Departments", "view")]
[HttpPost] ? [RequirePermission("Departments", "insert")]
[HttpPut("{id}")] ? [RequirePermission("Departments", "update")]
[HttpDelete("{id}")] ? [RequirePermission("Departments", "delete")]
```

### **3. EmployeesController (????????):**
```csharp
[HttpGet] ? [RequirePermission("Employees", "view")]
[HttpPost] ? [RequirePermission("Employees", "insert")]
[HttpPut("{id}")] ? [RequirePermission("Employees", "update")]
[HttpDelete("{id}")] ? [RequirePermission("Employees", "delete")]
```

### **4. WarehousesController (??????????):**
```csharp
[HttpGet] ? [RequirePermission("Warehouses", "view")]
[HttpPost] ? [RequirePermission("Warehouses", "insert")]
[HttpPut("{id}")] ? [RequirePermission("Warehouses", "update")]
[HttpDelete("{id}")] ? [RequirePermission("Warehouses", "delete")]
```

### **5. StatusesController (????? ??????):**
```csharp
[HttpGet] ? [RequirePermission("Assets", "view")]
[HttpPost] ? [RequirePermission("Assets", "insert")]
[HttpPut("{id}")] ? [RequirePermission("Assets", "update")]
[HttpDelete("{id}")] ? [RequirePermission("Assets", "delete")]
```

---

## ?? **???????? ??????:**

```cmd
crud-operations-comprehensive-test.bat
```

---

## ?? **??? ?????? CRUD ??? ????:**

### **????: ?????? Categories (??????):**

1. **???? ??? `/categories`**
2. **?????? VIEW:**
   - ??? ?? ???? ????? ??????
3. **?????? INSERT:**
   - ???? "Add New Category"
   - ???? ???????
   - ???? Save ? ??? ?? ????? ?????
4. **?????? UPDATE:**
   - ???? Edit ??? ??? ??????
   - ???? ????????
   - ???? Save ? ??? ?? ????? ?????
5. **?????? DELETE:**
   - ???? Delete ??? ???
   - ??? ????? ? ??? ?? ????? ?????

### **??? ??????? ????? ??????? ??????:**
- `/departments` (???????? ????????)
- `/employees` (????????)
- `/warehouses` (??????????)
- Asset Statuses ?? `/categories` tab "????? ??????"

---

## ?? **?????? ??????:**

### **? ??? ???????:**
- VIEW operations ???? ?
- INSERT/UPDATE/DELETE ???? 403 Forbidden ?

### **? ??? ???????:**
- ???? ???????? ???? ????? ??? ????????? ?
- ?? ???? ?? 403 errors ?
- Forms ???? ????? ????? ?

---

## ??? **???? ?????????:**

### **??????? ?????? ??? ?????:**

| ??????? | ???????? ???????? | ????? |
|---------|------------------|-------|
| ??? ??????? | `[Screen].view` | ?????? ???????? |
| ????? ???? | `[Screen].insert` | ????? ???? ???? |
| ????? ????? | `[Screen].update` | ????? ???????? |
| ??? ???? | `[Screen].delete` | ???/????? ?????? |

### **???? ???????? Categories:**
- `Categories.view` ? ?????? ????? ??????
- `Categories.insert` ? ????? ??? ?????
- `Categories.update` ? ????? ??? ??????
- `Categories.delete` ? ??? ???

---

## ?? **??????? ????????:**

**???? CRUD ?????? ????:**
- ? ???? ?????? ????????? ????
- ? ???? ???? ?? ?????????
- ? ????? ?????? ????
- ? ???? ???? ???? hardcoded roles
- ? ??? ???? ??????? ???????

**????? ???? ??????? ????????? ????!** ??