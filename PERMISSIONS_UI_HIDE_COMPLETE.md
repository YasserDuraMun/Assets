# ?? ????? ??????? ????? ??? ????????? - ???? ????

## ? ?? ?? ??????

?? ????? ???? ??????? ???? ?? ???? ???????? ??????? ?????? ????? ?? ????? ????? "Access Denied".

---

## ?? ??????? ???? ?? ???????/???????

### 1. ??? ????: `usePermission` Hook

**?????:** `ClientApp/src/hooks/usePermission.ts`

**???????:**
```typescript
// ?????? ?? ?????? ?????
const canDelete = usePermission('Assets', 'delete');

// ?????? ??? ???? ????????? ?????
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Assets');
```

**???????:**
- ? Super Admin ???? ???? ????????? ????????
- ? ????? ?? ??????? ???????? ?? `AuthContext`
- ? ???? 4 ????? ???????: `view`, `insert`, `update`, `delete`

---

### 2. ????? ???? Assets

**?????:** `ClientApp/src/pages/AssetsPage.tsx`

#### ?????????:

##### ?) Import Hook ??????
```typescript
import { useScreenPermissions } from '../hooks/usePermission';
```

##### ?) ??????? Hook
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Assets');
```

##### ?) ????? ?? "????? ???"
```typescript
{canInsert && (
  <Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/assets/add')}>
    ????? ???
  </Button>
)}
```

##### ?) ????? ????? ????????? ?? ??????
```typescript
render: (_: unknown, record: Asset) => {
  const actions = [];
  
  // ?? ??? (canView)
  if (canView) {
    actions.push(<Button key="view">???</Button>);
  }
  
  // ?? ????? (canUpdate)
  if (canUpdate) {
    actions.push(<Button key="edit">?????</Button>);
  }
  
  // ?? ??????? (canUpdate + ??? ??????)
  if (canUpdate && record.statusName !== 'Disposed') {
    actions.push(<Button key="dispose">???????</Button>);
  }
  
  // ?? ??? (canDelete)
  if (canDelete) {
    actions.push(<Popconfirm><Button>???</Button></Popconfirm>);
  }
  
  return actions.length > 0 ? <Space>{actions}</Space> : null;
}
```

---

### 3. ????? ???? Maintenance

**?????:** `ClientApp/src/pages/MaintenancePage.tsx`

#### ?????????:

##### ?) Import Hook
```typescript
import { useScreenPermissions } from '../hooks/usePermission';
```

##### ?) ??????? Hook
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Maintenance');
```

##### ?) ????? ?? "????? ?????"
```typescript
{canInsert && (
  <Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/assets')}>
    ????? ?????
  </Button>
)}
```

##### ?) ????? ????? ??????? ????????
```typescript
const getActionMenuItems = (record: AssetMaintenance) => {
  const items = [];
  
  // ?? ??? ????? (canView)
  if (canView) {
    items.push({ key: 'view', label: '??? ?????' });
  }
  
  // ?? ????? (canUpdate)
  if (canUpdate && (record.status === 1 || record.status === 2)) {
    items.push({ key: 'complete', label: '?????' });
  }
  
  // ?? ????? (canUpdate)
  if (canUpdate && record.status === 1) {
    items.push({ key: 'cancel', label: '?????' });
  }
  
  // ?? ??? (canDelete)
  if (canDelete) {
    items.push({ key: 'delete', label: '???' });
  }
  
  return items;
};
```

---

## ?? ????? ????? ??? ????? ????

### ?????? ??? ???? ????? (????? DisposalPage):

#### 1. Import Hook
```typescript
import { useScreenPermissions } from '../hooks/usePermission';
```

#### 2. ??????? Hook ?? Component
```typescript
export default function DisposalPage() {
  const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Disposal');
  // ...
}
```

#### 3. ????? ?? "Add New"
```typescript
{canInsert && (
  <Button type="primary" onClick={openAddModal}>
    ????? ???????
  </Button>
)}
```

#### 4. ????? ????? Actions
```typescript
{
  title: '?????????',
  render: (_, record) => {
    const actions = [];
    
    if (canView) actions.push(<Button>???</Button>);
    if (canUpdate) actions.push(<Button>?????</Button>);
    if (canDelete) actions.push(<Button>???</Button>);
    
    return actions.length > 0 ? <Space>{actions}</Space> : null;
  }
}
```

---

## ?? ??????? ???????? ???????

| ?????? | ?????? | ????????? |
|--------|--------|-----------|
| **Assets** | ? ?? ??????? | `Assets.view/insert/update/delete` |
| **Maintenance** | ? ?? ??????? | `Maintenance.view/insert/update/delete` |
| **Disposal** | ? ????? ????? | `Disposal.view/insert/update/delete` |
| **Transfers** | ? ????? ????? | `Transfers.view/insert/update/delete` |
| **Employees** | ? ????? ????? | `Employees.view/insert/update/delete` |
| **Departments** | ? ????? ????? | `Departments.view/insert/update/delete` |
| **Warehouses** | ? ????? ????? | `Warehouses.view/insert/update/delete` |
| **Categories** | ? ????? ????? | `Categories.view/insert/update/delete` |

---

## ?? ?????? ???????

### ????????? 1: Super Admin
```typescript
// Super Admin ???? ???? ?????????
{
  canView: true,
  canInsert: true,
  canUpdate: true,
  canDelete: true
}
```
**???????:** ??? ???? ??????? ?

---

### ????????? 2: Manager (?? ??? ?????????)
```typescript
// ?????: View + Insert ???
{
  canView: true,
  canInsert: true,
  canUpdate: false,
  canDelete: false
}
```
**???????:** 
- ? ??? ?? "???"
- ? ??? ?? "?????"
- ? **?? ???** ?? "?????"
- ? **?? ???** ?? "???"

---

### ????????? 3: Viewer (View ???)
```typescript
{
  canView: true,
  canInsert: false,
  canUpdate: false,
  canDelete: false
}
```
**???????:** 
- ? ??? ?? "???" ???
- ? **?? ???** ?? ????? ????

---

## ?? UX Improvements

### ??? ?
```
???????? ??? ???? ? ???? ???? ? ???? ??? "Access Denied"
```

### ??? ?
```
???????? ?? ??? ???? ????? ? ?? ????? ????? ? ?? ???? ????? ???
```

**???????:** ????? ?????? ???? ????? ??????!

---

## ?? ???? ?????????

### Frontend (UI)
- ? ????? ??????? ????? ??? ?????????
- ? ????? ????? ????????

### Backend (API) - ?????? ?????
- ? `[RequirePermission("Assets", "delete")]`
- ? ??????? ??? ????? API
- ? ??? ?? ????? ???????? Frontend? ???? ???? ?? Backend

**?????? ????? ???????:**
```
Frontend (UI Hide) ? Backend (API Protection) ? Database
```

---

## ?? ??????? ????

### 1. Super Admin ???
- ? Super Admin **??????** ??? ???? ???????
- ? ?? ????? ???????? ?????

### 2. ???? Actions ??????
- ??? ?? ??? ???? **??** ?????? ? ???? Actions ???? ????
- **???????:** ???? ????? ???? Actions ??????? ??? ??? ??????

### 3. ????????? Cascade
```typescript
// ?????: ??? ??? canUpdate = false
? ?? ??? ?? "?????"
? ?? ??? ?? "???????" (???? ????? canUpdate)
? ?? ??? ?? "?????" (?? Maintenance)
```

---

## ?? ??????? ???????

### 1. ????? ??????? ???????
```bash
# ????? ???? ????????? ???????
1. Super Admin ? ??? ?? ??? ?? ???
2. Manager ? ??? ?? ??? ??? ????????
3. Viewer ? ??? ?? ??? "???" ???
```

### 2. ??? ??? ??????? ????????
- Disposal
- Transfers
- Settings (Employees, Departments, etc.)

### 3. (???????) ????? ???? Actions
```typescript
// ??? ???? ????? ???? Actions ??????? ????? ???? ??????
const showActions = canView || canInsert || canUpdate || canDelete;

const columns = [
  // ... ????? ????
  ...(showActions ? [{
    title: '?????????',
    render: () => { /* ... */ }
  }] : [])
];
```

---

## ? ???????

- ? ?? ????? `usePermission` hook
- ? ?? ????? ??? AssetsPage ???????
- ? ?? ????? ??? MaintenancePage ???????
- ? ??????? **?????** ????? ?? ?????
- ? ????? ?????? ???? - ?? ????? "Access Denied"
- ? ?????? ????? ??? Backend

**???? ???????? ??? ??? ?? ?????? ????! ??**
