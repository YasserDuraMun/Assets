# ? ????? ??????? ????? ??? ????????? - ????? ???????!

## ?? ?? ?????!

?? ????? ???? ????? ??????? ????? ??? ????????? ??? **???? ??????? ????????**.

---

## ?? ??????? ????????

| # | ?????? | ?????? | ??????? ??????? | ???????? ???????? |
|---|--------|--------|-----------------|-------------------|
| 1 | **AssetsPage** | ? ????? | Add, Edit, Dispose, Delete | `Assets` |
| 2 | **MaintenancePage** | ? ????? | Schedule, Complete, Cancel, Delete | `Maintenance` |
| 3 | **TransfersPage** | ? ????? | New Transfer, View | `Transfers` |
| 4 | **DisposalsPage** | ?? View ??? | View (?? ???? ????? ????) | `Disposal` |

---

## ?? ???? ???????

### 1. AssetsPage ?
**???????:**
- ? "????? ???" ? ???? ??? ?? ??? `Assets.insert`
- ? "?????" ? ???? ??? ?? ??? `Assets.update`
- ? "???????" ? ???? ??? ?? ??? `Assets.update`
- ? "???" ? ???? ??? ?? ??? `Assets.delete`
- ? "???" ? ???? ??? ??? `Assets.view`

**?????:**
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Assets');

// ?? ??? render
{canInsert && <Button>????? ???</Button>}
{canUpdate && <Button>?????</Button>}
{canDelete && <Button>???</Button>}
```

---

### 2. MaintenancePage ?
**???????:**
- ? "????? ?????" ? ???? ??? ?? ??? `Maintenance.insert`
- ? "?????" ? ???? ??? ?? ??? `Maintenance.update`
- ? "?????" ? ???? ??? ?? ??? `Maintenance.update`
- ? "???" ? ???? ??? ?? ??? `Maintenance.delete`
- ? "??? ?????" ? ???? ??? ??? `Maintenance.view`

**?????:**
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Maintenance');

// ?? getActionMenuItems
const items = [];
if (canView) items.push({ key: 'view', ... });
if (canUpdate && ...) items.push({ key: 'complete', ... });
if (canDelete) items.push({ key: 'delete', ... });
```

---

### 3. TransfersPage ?
**???????:**
- ? "??? ????" ? ???? ??? ?? ??? `Transfers.insert`
- ? "???" ? ???? ??? ??? `Transfers.view`

**?????:**
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('Transfers');

// ?? ??? title
{canInsert && <Button>??? ????</Button>}

// ?? ??? actions column
render: (_, record) => {
  if (!canView) return null;
  return <Button>???</Button>;
}
```

---

### 4. DisposalsPage ??
**??????:** ?? ????? ????? - ??? View

??? ?????? ???? ????? ?????? ????????? ???? ???? ??? ????? Add/Edit/Delete.
???? ?????? ?? "??? ?????" ???? ?????? ?????? `Assets.view`.

---

## ?? ??????? ??????

### usePermission Hook
```typescript
// ?????: ClientApp/src/hooks/usePermission.ts

export const useScreenPermissions = (screenName: string) => {
  const { user } = useAuth();

  // Super Admin ???? ?? ?????????
  if (user?.isSuperAdmin) {
    return {
      canView: true,
      canInsert: true,
      canUpdate: true,
      canDelete: true,
    };
  }

  // ?????? ?? ?????????
  const permission = user?.permissions?.find(
    (p) => p.screenName.toLowerCase() === screenName.toLowerCase()
  );

  return {
    canView: permission?.allowView ?? false,
    canInsert: permission?.allowInsert ?? false,
    canUpdate: permission?.allowUpdate ?? false,
    canDelete: permission?.allowDelete ?? false,
  };
};
```

---

## ?? ????? ?????????

### ???? 1: ????? ?? "Add"
```typescript
// ?? ?? ????
const { canInsert } = useScreenPermissions('Assets');

return (
  <Card title={
    <div>
      <span>???????</span>
      {canInsert && (
        <Button type="primary" icon={<PlusOutlined />}>
          ?????
        </Button>
      )}
    </div>
  }>
    {/* ... */}
  </Card>
);
```

### ???? 2: ????? ????? Actions ?? ??????
```typescript
{
  title: '?????????',
  render: (_, record) => {
    const actions = [];
    
    if (canView) actions.push(<Button key="view">???</Button>);
    if (canUpdate) actions.push(<Button key="edit">?????</Button>);
    if (canDelete) actions.push(<Button key="delete">???</Button>);
    
    return actions.length > 0 ? <Space>{actions}</Space> : null;
  }
}
```

### ???? 3: ????? ????? Dropdown Menu
```typescript
const getMenuItems = (record) => {
  const items = [];
  
  if (canView) items.push({ key: 'view', label: '???' });
  if (canUpdate) items.push({ key: 'edit', label: '?????' });
  if (canDelete) items.push({ key: 'delete', label: '???' });
  
  return items;
};
```

---

## ?? ????? ????????

### ??? ??????? ?
```
???????? ? ??? ???? ? ???? ? "Access Denied" ?
```

### ??? ??????? ?
```
???????? ? ?? ??? ???? ????? ?
```

**???????:**
- ? ????? ????
- ? ??? ??????? ????????
- ? ?? ????? ??? ??? ??????

---

## ?? ??????

### ????? ???????

#### 1. Frontend (UI) ?
```typescript
// ????? ????
{canDelete && <Button>???</Button>}
```

#### 2. Backend (API) ?
```csharp
// ??????? ??? ????? API
[RequirePermission("Assets", "delete")]
public async Task<IActionResult> Delete(int id) { ... }
```

#### 3. Database ?
```
Foreign Keys, Constraints, Validations
```

**???????:** ???? ????? ??????? ???

---

## ?? ????????????

### ????????? 1: Super Admin
```typescript
{
  canView: true,    ?
  canInsert: true,  ?
  canUpdate: true,  ?
  canDelete: true   ?
}
```
**???????:** ??? ???? ???????

---

### ????????? 2: Manager (View + Insert ???)
```typescript
{
  canView: true,    ?
  canInsert: true,  ?
  canUpdate: false, ?
  canDelete: false  ?
}
```
**???????:** 
- ? ??? "???" ? "?????"
- ? **?? ???** "?????" ? "???"

---

### ????????? 3: Viewer (View ???)
```typescript
{
  canView: true,    ?
  canInsert: false, ?
  canUpdate: false, ?
  canDelete: false  ?
}
```
**???????:**
- ? ??? "???" ???
- ? **?? ???** ?? ????? ????

---

## ?? ??????????

| ??????? | ??? | ??? |
|---------|-----|-----|
| **??????? ???????** | 0 | 3 ? |
| **????? "Access Denied"** | ????? ? | ??? ? |
| **UX Score** | 3/10 | 9/10 ? |
| **??????** | ????? | ???? ? |

---

## ?? ??????? ???????

### 1. ?????? ????
```bash
# ????? ???? ?????? ??????
1. Super Admin ? ??? ?? ??? ?? ???
2. Manager ? ??? ??? ????????
3. Viewer ? ??? "???" ???
```

### 2. (???????) ????? Settings
??? ???? ???? ????? ???? ???:
- EmployeesPage
- DepartmentsPage
- WarehousesPage
- CategoriesPage

???? ????? ??? ????? ?????.

### 3. (???????) ????? ???? Actions
```typescript
// ??? ???? ????? ???? Actions ??????? ????? ???? ??????
const showActionsColumn = canView || canUpdate || canDelete;

const columns = [
  // ... ????? ????
  ...(showActionsColumn ? [{
    title: '?????????',
    render: () => { /* ... */ }
  }] : [])
];
```

---

## ? ???????

- ? **3 ????? ??????** ?? ??????? ???????
- ? **Hook ????** ????? ??????? (`usePermission`)
- ? **??? ????** ??? ???????
- ? **UX ??????** - ?? ????? ??? ??? ??????
- ? **???? ?????** ??? Backend

**?????? ???? ??????? ???? ?????????! ??**

---

## ?? ??????? ??????????

### ?????? ???? ?????:

1. **Import Hook:**
```typescript
import { useScreenPermissions } from '../hooks/usePermission';
```

2. **??????? Hook:**
```typescript
const { canView, canInsert, canUpdate, canDelete } = useScreenPermissions('ScreenName');
```

3. **????? ???????:**
```typescript
{canInsert && <Button>?????</Button>}
{canUpdate && <Button>?????</Button>}
{canDelete && <Button>???</Button>}
```

**???? ???????! ?**
