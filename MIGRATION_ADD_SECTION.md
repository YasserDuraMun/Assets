# ????? Migration ?????? CurrentSectionId

## ?????:

```powershell
dotnet ef migrations add AddCurrentSectionIdToAsset
```

## ????? ??? ????? ????????:

```powershell
dotnet ef database update
```

## ?? ???? ??????:

1. **???? ????** ?? ???? `Assets`:
   - `CurrentSectionId` (int, nullable)

2. **Foreign Key** ????:
   - ?? `Assets.CurrentSectionId` ??? `Sections.Id`

## ???????:

?? **Package Manager Console**:

```powershell
# 1. ???? ???????
# 2. ???:
dotnet ef migrations add AddCurrentSectionIdToAsset
dotnet ef database update

# 3. ??? ??????? ?? ????
```

---

## SQL ??????? (??? ???? ?????? ??????):

```sql
USE Assets;
GO

-- ????? ???? CurrentSectionId
ALTER TABLE Assets
ADD CurrentSectionId INT NULL;
GO

-- ????? Foreign Key
ALTER TABLE Assets
ADD CONSTRAINT FK_Assets_Sections_CurrentSectionId
FOREIGN KEY (CurrentSectionId) REFERENCES Sections(Id);
GO

-- ??????
SELECT 
    COLUMN_NAME, 
    DATA_TYPE, 
    IS_NULLABLE
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'Assets' 
  AND COLUMN_NAME = 'CurrentSectionId';
GO
```
