# ?? ???? ????? Frontend ?? ?????? ??????

## ? **?? ?? ??????:**

### 1. **API Client (securityApi.ts):**
- ? JWT Token management
- ? Auto token refresh
- ? Auth, Users, Permissions APIs
- ? Error handling ????

### 2. **TypeScript Types (security.ts):**
- ? User, Role, Permission interfaces
- ? API Request/Response types
- ? Auth types ??????

### 3. **Auth Context (AuthContext.tsx):**
- ? JWT Authentication
- ? Permission checking
- ? Role management
- ? Persistent login

### 4. **React Components:**
- ? **Login.tsx** - ????? ?????? ?? JWT
- ? **UserManagement.tsx** - ????? ?????????? ????????
- ? **PermissionGuard.tsx** - ????? ???????? ??????????
- ? **Dashboard.tsx** - ???? ???? ?? Permissions

### 5. **App Integration:**
- ? Protected Routes
- ? Permission-based navigation
- ? Role-based access control

---

## ?? **????? ???????:**

### ?????? 1: ????? Dependencies
```bash
cd ClientApp
npm install
```

### ?????? 2: ????? package.json
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.0",
    "axios": "^1.3.0",
    "antd": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "typescript": "^4.9.0",
    "vite": "^4.1.0"
  }
}
```

### ?????? 3: ????? Environment Variables
```bash
# ClientApp/.env
VITE_API_BASE_URL=http://localhost:5002

# ClientApp/.env.production  
VITE_API_BASE_URL=https://localhost:44385
```

### ?????? 4: ????? Tailwind CSS (???????)
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

```css
/* ClientApp/src/index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

---

## ?? **????? ?????????:**

### 1. **????? ??????:**
```tsx
import Login from './components/Login';

// ?????????
<Login />

// ?????? ?????? ??????????:
// Email: admin@assets.ps
// Password: Admin@123
```

### 2. **????? ??????? ??????????:**
```tsx
import PermissionGuard from './components/PermissionGuard';

<PermissionGuard screenName="Users" action="view">
  <UserManagement />
</PermissionGuard>
```

### 3. **????? ????????:**
```tsx
import { RoleGuard } from './components/PermissionGuard';

<RoleGuard roles={['Super Admin', 'Admin']}>
  <AdminPanel />
</RoleGuard>
```

### 4. **????? ??????????:**
```tsx
import UserManagement from './components/UserManagement';

// Features:
// - ??? ???? ?????????? ?? ???????
// - ????? ?????? ???? ?? ?????? ???????
// - ????? ?????? ????????
// - ?????/????? ????? ????????
// - ??? ???????? (?? ????? ??????)
```

### 5. **??????? Auth Context:**
```tsx
import { useAuth } from './contexts/AuthContext';

const MyComponent = () => {
  const { user, hasPermission, hasRole, logout } = useAuth();

  const canEdit = hasPermission('Assets', 'update');
  const isAdmin = hasRole('Admin');

  return (
    <div>
      <h1>?????? {user?.fullName}</h1>
      {canEdit && <button>?????</button>}
      {isAdmin && <AdminButton />}
    </div>
  );
};
```

---

## ?? **???????? ???????:**

### 1. **Login Screen:**
- ????? ???? ?? JWT
- ?????? ????? ????? ??????
- ????? ????? ???????

### 2. **Dashboard:**
- ???? ???? ??????
- ???????? ????? ??? ?????????
- ????? ????? ?? Permission Guards
- ??? ??????? ???????? ??????

### 3. **User Management:**
- ???? ?????????? ?? ???????
- ?????/?????/??? ??????????
- ????? ????? ?????? ????????
- ?????/????? ????? ??????????

### 4. **Permission System:**
- ??? ????????? ??? ????? ???????
- 4 ????? ???????: View, Insert, Update, Delete
- ????? ???????? ?????????
- Role-based access control

---

## ?? **??????? ??????:**

### ????? ?????? ?? ???????:
```tsx
// ????? ???? ????? ?? ?????
<Route 
  path="/categories" 
  element={
    <PermissionGuard screenName="Categories" action="view">
      <CategoriesPage />
    </PermissionGuard>
  } 
/>
```

### ????? Permission ????????:
```tsx
const MyComponent = () => {
  const { hasPermission } = useAuth();

  return (
    <div>
      {hasPermission('Assets', 'insert') && (
        <button onClick={addAsset}>????? ???</button>
      )}
      
      {hasPermission('Assets', 'update') && (
        <button onClick={editAsset}>?????</button>
      )}
      
      {hasPermission('Assets', 'delete') && (
        <button onClick={deleteAsset}>???</button>
      )}
    </div>
  );
};
```

---

## ?? **??????? ????????:**

**? Frontend ???? ?? ???? ???? ?????:**

- ? **JWT Authentication** ?? auto-refresh
- ? **Role-based Access Control** - 5 ?????
- ? **Screen-level Permissions** - 4 ????? ???????  
- ? **User Management Interface** - CRUD ????
- ? **Protected Routes** ? Components
- ? **Responsive Design** - ???? ??? ???? ???????
- ? **TypeScript** - Type safety ????
- ? **Error Handling** - ?????? ????? ???????

**?????? ???? ??????? ??????????!** ??

---

## ?? **??????? ???????:**

1. ????? `npm start` ?? ClientApp
2. ????? Backend ??? port 5002
3. ??? `http://localhost:3000`
4. ????? ?????? ???????? admin@assets.ps
5. ?????? ????? ?????????? ??????????

**?? ??? ???? ?????!** ?