# ???? ????????? - NullReferenceException ?? CreateMaintenance

## ??????? ????????

???? ??????? ?? `MaintenanceService.CreateAsync()` ????? `null` ??? ????? ??? ??????? ?????? ??? ???? `NullReferenceException` ??? ?????? ?????? ??? `maintenance.Id`.

## ????? ??????

?????? ?????? ?????? ??????????:
- **User ??????** (`Assets.Models.User`) - ??????? ?? ????? ??????
- **SecurityUser ??????** (`Assets.Models.Security.User`) - ???????? ?? ???? ????????? ????????

??????? ????:
1. ???????? ?????? (userId=6) ????? ?? ???? `SecurityUsers`
2. ??? `AssetMaintenance.Creator` ??? ???? ??? `Assets.Models.User` (?????? ?????? `Users`)
3. ??? ?????? ????? `AssetMaintenance` ?? `.Include(m => m.Creator)` ?? `GetByIdAsync()`? ??? Entity Framework ?? ????? ???????? ???? ???? ?? ?????? ?????
4. ??? ??? ??? ????? `null` ?? `GetByIdAsync()`

## ???? ??????

### 1. ????? ??????? (Models)

#### ? AssetMaintenance.cs
```csharp
using Assets.Models.Security;  // ?????

public Models.Security.User Creator { get; set; } = null!;  // ????? ?? User ??????
```

#### ? AssetMovement.cs
```csharp
using Assets.Models.Security;  // ?????

public User PerformedByUser { get; set; } = null!;  // ???? ???? ??? SecurityUser
```

#### ? AssetDisposal.cs
```csharp
using Assets.Models.Security;  // ?????

public User PerformedByUser { get; set; } = null!;  // ???? ???? ??? SecurityUser
```

#### ? SecurityUser (Models/Security/User.cs)
```csharp
// ????? navigation properties ???????? ???????
public ICollection<AssetMaintenance> CreatedMaintenances { get; set; } = new List<AssetMaintenance>();
public ICollection<AssetMovement> PerformedMovements { get; set; } = new List<AssetMovement>();
public ICollection<AssetDisposal> PerformedDisposals { get; set; } = new List<AssetDisposal>();
```

### 2. ????? ApplicationDbContext

#### ? AssetMaintenance Configuration
```csharp
entity.HasOne(e => e.Creator)
    .WithMany(u => u.CreatedMaintenances)  // ????? ?? .WithMany()
    .HasForeignKey(e => e.CreatedBy)
    .OnDelete(DeleteBehavior.Restrict);
```

#### ? AssetMovement Configuration
```csharp
entity.HasOne(e => e.PerformedByUser)
    .WithMany(u => u.PerformedMovements)  // ????? ?? .WithMany()
    .HasForeignKey(e => e.PerformedBy)
    .OnDelete(DeleteBehavior.Restrict);
```

#### ? AssetDisposal Configuration
```csharp
entity.HasOne(e => e.PerformedByUser)
    .WithMany(u => u.PerformedDisposals)  // ????? ?? .WithMany()
    .HasForeignKey(e => e.PerformedBy)
    .OnDelete(DeleteBehavior.Restrict);
```

### 3. ????? MaintenanceService

#### ? GetByIdAsync - ????? null check
```csharp
CreatedByName = maintenance.Creator?.FullName ?? "Unknown",  // ????? ?.
```

#### ? CreateAsync - ????? ?????? ?? ???????? ??????? ???????
```csharp
public async Task<MaintenanceDto> CreateAsync(CreateMaintenanceDto dto, int userId)
{
    // ?????? ?? ???? ?????
    var asset = await _context.Assets.FindAsync(dto.AssetId);
    if (asset == null)
        throw new Exception("????? ??? ?????");

    // ?????? ?? ???? ???????? ?? SecurityUsers ? ????
    var user = await _context.SecurityUsers.FindAsync(userId);
    if (user == null)
        throw new Exception("???????? ??? ?????");

    // ... ????? ??????? ...
    
    await _context.SaveChangesAsync();
    
    var result = await GetByIdAsync(maintenance.Id);
    
    // ?????? ?? ??????? ? ????
    if (result == null)
    {
        _logger.LogError("Failed to retrieve maintenance record {Id} after creation", maintenance.Id);
        throw new Exception($"??? ?? ??????? ??? ??????? ??? ???????. ID: {maintenance.Id}");
    }
    
    return result;  // ????? ?? return (await GetByIdAsync(maintenance.Id))!
}
```

#### ? UpdateAsync ? CompleteMaintenanceAsync
- ????? ??? ?????? ??????? ?????? ?? ???????

## ???????

1. ? **????? ???? ??????????** - ?? ??? ???? ?????? SecurityUser
2. ? **????? ?????? ???????** - ????? ??? ????? ????? ?? NullReferenceException
3. ? **?????? ?? ????????** - ?????? ?? ???? ???????? ??? ???????
4. ? **?????? ?????** - navigation properties ?? ?????????
5. ? **????? ?? ???? ?????????** - SecurityUser ?? ???????? ?? ???? ??????? ??????????

## ??????? ???????

1. ?? **????? ?????? Migration** (???? MIGRATION_INSTRUCTIONS.md)
2. ? **?????? ????? ??? ????? ????**
3. ? **?????? ?? ??? ?? endpoints ?? MaintenanceController**

## ??????? ????????

??? ????? ??? ?????????:
- ? ?? ???? `NullReferenceException` ??? ????? ??? ?????
- ? ???? ??? ??? ???????? ???? ???? ????? ???? ????
- ? ?? ???????? ??? ????? ??????? ????? ???? ????
- ? ?????? ???? ???????? SecurityUser ?? ?? ????
